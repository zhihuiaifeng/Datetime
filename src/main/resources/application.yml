#开发环境配置
server:
  #端口
  port: 8880
  tomcat:
    uri-encoding: utf-8
#日志配置
logging:
  level: #日志的级别，将com.yj.oa.project.mapper包下的级别调为debug
    com.yj.oa.project.mapper: debug
#数据源
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/accentcount?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: 150022
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
